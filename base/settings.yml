# Testing
deployment:
  domain: andrewmyshok.com
  ports:
    port: 8080
    protocol: tcp
  ssl:
    directories:
      cert: /etc/ssl/private/
      key: /etc/ssl/certs/
      acme-challenge: /etc/nginx/acme/.well-known/acme-challenge